apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app: postgres-data
  name: postgres-pv-volume
  namespace: {{ .Values.namespace }}
spec:
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
  storageClassName: gp2 