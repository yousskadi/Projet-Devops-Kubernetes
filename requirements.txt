# Core FastAPI dependencies
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
email-validator==2.3.0  # required by pydantic EmailStr

# Database
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12
alembic==1.18.4  # Database migrations

# Security
bcrypt==5.0.0  # Password hashing (used directly, passlib removed: unmaintained)
python-dotenv==1.2.2  # Environment variables

# HTTP client for testing
httpx==0.28.1

# Development tools
black==26.5.1
ruff==0.15.17  # Fast Python linter
mypy==2.1.0
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1

# Observability
prometheus-client==0.25.0
prometheus-fastapi-instrumentator==8.0.0

# Removed (unused in code, were flagged by Trivy/Dependabot):
# - python-jose (JWT not implemented yet; re-add when needed)
# - passlib (unmaintained, replaced by direct bcrypt usage)
# - pydantic-settings (unused)
# - opentelemetry-* (unused, exporter version was yanked)
